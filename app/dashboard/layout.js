export const metadata = {
  title: "Dashboard",
  description: "Track your Lightning purchases and sales on AfriSats.",
};

export default function DashboardLayout({ children }) {
  return children;
}
