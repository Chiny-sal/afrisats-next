export const metadata = {
  title: "Sell",
  description: "Register as a seller and list digital or physical products on AfriSats.",
};

export default function SellLayout({ children }) {
  return children;
}
