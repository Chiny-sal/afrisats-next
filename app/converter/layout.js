export const metadata = {
  title: "Sats Converter",
  description: "Bidirectional satoshi to African fiat currency converter with live rates.",
};

export default function ConverterLayout({ children }) {
  return children;
}
